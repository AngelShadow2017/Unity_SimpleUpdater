using UnityEngine;
[DefaultExecutionOrder(int.MinValue)]
public class UpdaterFlag : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        Debug.Log("UpdateFlag");
    }
}
